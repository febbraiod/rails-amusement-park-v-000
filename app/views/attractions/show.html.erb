<%= @attraction.name %>
<%= @attraction.tickets %>
<%= @attraction.nausea_rating %>
<%= @attraction.happiness_rating %>
<%= @attraction.min_height %>

<%= button_to "Go on this ride", {:controller => "attractions", :action => "ride", :attraction_id => @attraction.id, :method=>:post} unless @user.admin %>

<%= link_to "Edit Attraction", edit_attraction_path(@attraction) if @user.admin %>